export io.github.llh4github.lotus.model.auth.User

input UserInput{
   #allScalars(this)
}

// 用于查询用户权限信息的view
UserAuthView {
    #allScalars(this)
    id
    username
    roles{
        title
        code
        urlResources{
            method
            path
        }
    }
}

specification UserSimpleQuerySpec {
    like(username)
}
